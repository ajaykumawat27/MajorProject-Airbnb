<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust</title>
</head>

<body>
    <h3>Listing Details : </h3>
    <ul>
        <li>
            <%= listing.title %>
        </li>
        <li>
            <%= listing.description %>
        </li>
        <li>
            <!-- toLocaleString is function converts a number to string with-->
            <%= listing.price.toLocaleString("en-IN", { style: "currency" , currency: "INR" }) %>
        </li>

        <li>
            <%= listing.location %>
        </li>
        <li>
            <%= listing.country %>
        </li>

    </ul>
    <br><br>
    <!-- when we click on Edit the href link will be activated for that id in which show.ejs is currently opened 
     or jaise he es link par jaayenge vaise he Edit Route got activated in app.js-->
    <a href="/listings/<%= listing._id %>/edit">Edit</a> <!--edit k liye alag se form banaya hai edit.ejs main h-->
    <br><br>
    <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
        <button>Delete</button>
    </form>
</body>

</html>